import { createTheme } from '@mui/material/styles';

// Create a theme instance.
const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2', // You can customize this
    },
    secondary: {
      main: '#dc2626', // You can customize this
    },
  },
});

export default theme;
